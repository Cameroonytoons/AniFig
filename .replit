modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Build Figma Plugin"

[[workflows.workflow]]
name = "Build Figma Plugin"
author = "agent"

[workflows.workflow.metadata]
agentRequireRestartOnSave = false

[[workflows.workflow.tasks]]
task = "packager.installForAll"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = """

    echo \"Cleaning build directory...\" &&
    rm -rf build/* &&
    echo \"Creating build directory structure...\" &&
    mkdir -p build/src &&
    echo \"Running TypeScript compilation...\" &&
    npx tsc --project tsconfig.json --listEmittedFiles &&
    echo \"Building UI bundle...\" &&
    npx esbuild src/ui.ts --bundle --platform=browser --target=es2019 --format=iife --global-name=UI --outfile=build/ui.js --sourcemap --log-level=warning &&
    echo \"Building plugin code...\" &&
    npx esbuild code.ts --bundle --platform=node --target=es2019 --outfile=build/code.js --sourcemap --log-level=warning &&
    echo \"Build completed successfully\" &&
    ls -la build/
    """

[deployment]
run = ["sh", "-c", """

    echo \"Cleaning build directory...\" &&
    rm -rf build/* &&
    echo \"Creating build directory structure...\" &&
    mkdir -p build/src &&
    echo \"Running TypeScript compilation...\" &&
    npx tsc --project tsconfig.json --listEmittedFiles &&
    echo \"Building UI bundle...\" &&
    npx esbuild src/ui.ts --bundle --platform=browser --target=es2019 --format=iife --global-name=UI --outfile=build/ui.js --sourcemap --log-level=warning &&
    echo \"Building plugin code...\" &&
    npx esbuild code.ts --bundle --platform=node --target=es2019 --outfile=build/code.js --sourcemap --log-level=warning &&
    echo \"Build completed successfully\" &&
    ls -la build/
    """]
